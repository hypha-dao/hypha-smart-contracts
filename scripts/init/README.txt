This are some tools to get a docker based DUNES install up and running

run_dunes starts a node on the docker container

create_all_accounts creates necessary accounts for hypha contracts to install, most importantly owner account